@{
    var dataMain = ViewBag.BannerLocation1 as List<Web_GemsPortal.ViewModels.Banner.VM_BannerList>;
}
@*
<div class="container banner-slider">
    <div class="row justify-content-center" data-aos="zoom-in-down" data-aos-duration="600">
        <div class="col-md-8 mb-2">
            <div class="row px-1">
                <div class="swiper banner_slide_top_left">
                    <div class="swiper-wrapper">
                        @if (dataMain != null && dataMain.Any())
                        {
                            foreach (var item in dataMain)
                            {
                                <div class="swiper-slide">
                                    <a target="_blank" href="@item.url" class="w-100 h-100 spotlight">
                                        <img loading="lazy" src="@item.imageUrl" alt="@item.title"
                                             onerror="this.onerror=null;this.src='/assets/images/error/home_bannertop.png';"/>
                                    </a>
                                </div>
                            }
                        }
                    </div> *@
@*<div class="swiper-pagination"></div>*@
@* <div class="swiper-button-next"></div>
                    <div class="swiper-button-prev"></div>
                </div>
            </div>
        </div> *@
@* <div class="col-md-4 mb-2">
            <div class="row px-1">
                <div class="swiper banner_slide_top_right1">
                    <div class="swiper-wrapper">
                        @if (dataRight1 != null && dataRight1.Any())
                        {
                            foreach (var item in dataRight1)
                            {
                                <div class="swiper-slide">
                                    <a target="_blank" href="@item.url" class="w-100 h-100 spotlight">
                                        <img loading="lazy" src="@item.imageUrl" alt="@item.title"
                                             onerror="this.onerror=null;this.src='/assets/images/error/home_bannertop.png';"/>
                                    </a>
                                </div>
                            }
                        }
                    </div> *@
@*  <div class="swiper-button-next"></div>
                    <div class="swiper-button-prev"></div> *@
@* <div class="swiper-pagination"></div>
                </div>
            </div> *@
@*  <div class="row px-1 mt-2">
                <div class="swiper banner_slide_top_right2">
                    <div class="swiper-wrapper">
                        @if (dataRight2 != null && dataRight2.Any())
                        {
                            foreach (var item in dataRight2)
                            {
                                <div class="swiper-slide">
                                    <a target="_blank" href="@item.url" class="w-100 h-100 spotlight">
                                        <img loading="lazy" src="@item.imageUrl" alt="@item.title"
                                             onerror="this.onerror=null;this.src='/assets/images/error/home_bannertop.png';"/>
                                    </a>
                                </div>
                            }
                        }
                    </div>

                    <div class="swiper-pagination"></div>
                </div>
            </div> *@
@*  </div>
    </div>
</div>
 *@
<div class="justify-content-center">
    <div class="swiper banner_slide_location_1">
        <div class="swiper-wrapper">
            @if (dataMain != null && dataMain.Any())
            {
                foreach (var item in dataMain)
                {
                    <div class="swiper-slide">
                        <a target="_blank" href="@item.url" class="w-100 h-100">
                            <img class="aspect_ratio_16_6"  src="@item.imageUrl" alt="@item.title"
                                 onerror="this.onerror=null;this.src='/assets/images/error/home_bannertop.png';" />
                        </a>
                    </div>
                }
            }
        </div>
        <!-- If we need navigation buttons -->
        <div class="swiper-button-prev"><i class="fa fs-2 text-white fa-chevron-circle-left" aria-hidden="true"></i></div>
        <div class="swiper-button-next"><i class="fa fs-2 text-white fa-arrow-circle-right" aria-hidden="true"></i></div>
    </div>
</div>
